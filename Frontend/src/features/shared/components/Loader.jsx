import "../styles/loader.scss"

const Loader = () => {
  return (
    <main className="loader-state">
        <div id="loader-content">
            <p>Loading...</p>
        </div>
    </main>
  )
}

export default Loader
